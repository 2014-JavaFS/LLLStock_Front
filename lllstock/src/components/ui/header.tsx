const Header = () => {
    return (
        <header className="bg-gradient-to-r from-gray-700 via-gray-600 to-gray-800 text-center py-4 flex items-center justify-center">
            <img 
                src="/LLLMD_Logo.svg" 
                alt="Logo" 
                className="h-60"
            />
        </header>
    );
};

export default Header;
